{
  "name": "cestak-notifications-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test:cron": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
